# Spring Batch XML Item Reader

A Spring Batch sample application that reads XML fragments.

It uses [Maven POM](https://maven.apache.org/pom.html) as the XML schema and the project's POM as an input file.
